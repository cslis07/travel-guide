# 변경 이력 — 트립가이드

배포 URL: https://travel-guide-cslis07.vercel.app · GitHub `cslis07/travel-guide` (`master` push → Vercel 자동 배포)

> 2026-08-05 신규 작성. 이전 항목은 git log에서 소급 복원했으며, 사용자가 체감한 변화 위주로 옮겼다.

---

## 2026-08-05 (2) — 보안 점검 및 수정

### 🚀 배포
- 프로덕션 배포 (`e7c510a`) — 스모크 27/27 통과, 공격 시도로 차단 실측 완료

### 🔒 취약점 수정
- **[심각] SSRF — 공개 오픈 프록시로 악용 가능했음**
  `api/proxy.js`의 `_url` 파라미터가 **임의 URL을 검증 없이 fetch** 했다.
  누구나 `/api/proxy?_url=<아무 주소>`로 이 Vercel 함수를 익명 프록시처럼 쓸 수 있어
  대역폭 도용·내부 주소(예: `169.254.169.254` 메타데이터) 탐색·제3자 공격 경유가 가능했다.
  실제로 필요한 곳은 Open-Meteo 2개 호스트뿐이라 **허용 목록 + https 강제**로 제한.
  `path` 파라미터도 정규식(`Word/Word`)으로 검증해 경로 탈출을 막았다.
  실측: `example.com`·AWS 메타데이터 → **403**, `path=../../evil` → **400**, 정상 경로·Open-Meteo → **200**.

- **[심각] 저장형 XSS — 악성 링크 한 번으로 홈에서 코드 실행**
  `main.js`에 **이스케이프 함수 자체가 없었다.** TourAPI·localStorage 값을
  `alt="${item.title}"`, `data-cid="${item.contentid}"` 처럼 속성에 직접 삽입했고,
  특히 `onclick="toggleSavePlace('${item.contentid}',event)"`는 JS 문자열 주입까지 열려 있었다.
  여기에 `/mytrip`의 `#data=` 가져오기가 결합해 **완전한 공격 체인**이 성립했다 —
  악성 링크 → 사용자가 '가져오기' 클릭 → localStorage 오염 → 홈 방문 시 실행.
  `esc()`/`safeUrl()`/`safeText()` 추가 후 **17곳에 적용**, 인라인 `onclick`은
  `this.dataset.cid` 참조로 바꿔 주입 벡터 자체를 제거했다. (`main.js`)

- **주입 진입점 차단** — `/mytrip` 가져오기가 URL 데이터를 무검증 저장했다.
  허용 키·문자열 타입·200KB 상한·JSON 파싱 가능 여부를 검증하도록 변경. (`mytrip.html`)

- **보안 헤더 추가**(`vercel.json`) — CSP, Permissions-Policy, HSTS.
  인라인 스크립트가 많은 정적 사이트라 `script-src`에 `'unsafe-inline'`이 불가피하지만,
  **외부 도메인 스크립트 로드와 데이터 유출 경로는 차단**된다.
  코드가 쓰는 외부 리소스(jsdelivr·googletagmanager·open-er-api·구글폰트)를
  전수 교차검증해 기존 기능이 막히지 않음을 확인했다.

---

## 2026-08-05

### 🚀 배포
- 프로덕션 배포 (`b14132c`) — 스모크 테스트 27/27 통과

### 🎨 UI 전면 개편 — 여행앱 패턴 적용
- **모바일에 하단 탭바 도입** (홈·여행지·투어티켓·내 여행·공항)
  기존에는 상단 햄버거 메뉴라 이동에 두 번 탭 + 손 위치 이동이 필요했다.
  Apple HIG·Material Design 3 모두 핵심 목적지 3~5개를 엄지 반경인 하단에 두길 권장하며,
  여행자는 낯선 곳에서 한 손으로 쓰므로 이 패턴이 맞다.
  `nav.js` — CSS를 자체 포함해 `style.css`를 안 쓰는 페이지(airport/guide)에서도 동작.
  iOS 홈 인디케이터 `safe-area-inset-bottom` 대응, 목적지 13곳은 '여행지' 탭이 활성화된다.
- **이모지 → 선(line) 아이콘 교체** — 이모지는 OS·브라우저마다 모양이 달라 톤이 제각각이었다.
  Lucide 스타일(24×24, stroke 1.75) 인라인 SVG 51종을 `icons.js`로 자체 제작.
  CDN·아이콘 폰트 의존이 없어 오프라인·CSP에서 안전하다.
- **디자인 토큰 v2** (`style.css`) — 그레이 10단계 세분화, 카드 radius 12→16px,
  그림자를 2겹(얇은 윤곽 + 넓고 옅은 확산)으로 교체해 앱 질감 확보.
- **서체 Pretendard 적용** — 가격·시간·편명 등 숫자를 많이 읽는 화면이라
  숫자 가독성이 Noto Sans KR보다 나은 쪽을 택했다. `<link>`로 병렬 로드
  (처음엔 `@import`로 넣었다가 style.css 파싱 후에야 요청돼 렌더가 지연되는 문제로 교체).
- **터치 타깃 44px 이상 + pill 버튼 + 누름 피드백**(`:active` 축소) — Apple HIG 최소 규격.
- 모바일에서 상단 nav·햄버거 제거 → 탭바로 일원화, 화면이 단순해졌다.

### 🔧 수정
- **푸터 목적지 링크 111건을 전용 가이드 페이지로 교체** — `tours.html?city=오사카` 검색 이동이던 것을
  `osaka.html` 등 실제 가이드로 연결. 13곳 전부 페이지가 생겼는데 푸터만 옛 링크로 남아 있었다.
- **푸터에 '이용가이드' 링크 추가**(18곳) — 모바일에서 상단 nav를 숨기면서
  탭바에 없는 이용가이드로 가는 경로가 끊기는 문제를 발견해 함께 조치.
- 하단 고정 요소(`#pwa-install-btn`, `#icn-toast`)가 탭바에 가려지지 않도록 위로 올림.

---

## 2026-08-04

### 🚀 배포
- 프로덕션 배포 (`b0c7564`)

### ✨ 추가
- **인천공항 '공항 가는 길'에 서울역 도심공항터미널 안내** — 서울역 지하 2층에서
  탑승 수속·수하물 위탁·출국심사를 미리 마치고 인천공항 3층 전용 통로로 줄 없이 출국하는 경로.
  이용 항공사 8곳, 직통열차 T1 43분/T2 51분, 2026-08-03 리뉴얼(순번 대기시스템) 반영. (`airport.html`)
- 공항철도 직통 요금 최신화 — 성인 13,000원 / 어린이 9,500원, 일반열차 T2 5,350원.

---

## 2026-07-29

### 🚀 배포
- 프로덕션 배포 (`32c2d24`, `05c2cf9`)

### 🔒 보안·정책
- **AI 크롤러 수집 차단** — GPTBot·ClaudeBot·Google-Extended·PerplexityBot·CCBot·Bytespider 등
  20종을 `robots.txt`에서 `Disallow`, 전역 헤더 `X-Robots-Tag: noai, noimageai` 추가(`vercel.json`).
  일반 검색엔진 색인은 유지되므로 SEO에는 영향이 없다.
  ⚠️ 자발적 준수 기반이라 악성 스크래퍼와 사람의 복사는 막지 못한다.

### ✨ 추가
- **삿포로 가이드** 추가 — 눈축제·오타루·게요리·조잔케이 온천. 오사카식 3유형 구조.

---

## 2026-07-21

### 🚀 배포
- 프로덕션 배포 (`76147ea`, `6e1e213`)

### 🎨 개선
- **투어·티켓/숙소 검색 결과를 카드로 교체** — 이전에는 마이리얼트립이 준 `copy_text`
  마크다운 덩어리를 그대로 뿌려 번호 목록처럼 보였고 이미지·예약 링크가 없었다.
  응답 안의 위젯 트리(`data.widget.children[]`)를 파싱해 썸네일·평점·리뷰수·가격·뱃지·예약링크를
  카드로 렌더링. **5개씩 페이지네이션**을 붙여 한 번에 쏟아지는 부담을 줄였다. (`tours.html`)
  위젯 트리는 마이리얼트립 자사 렌더러용이라 스키마가 바뀔 수 있어, 파싱 실패 시 `copy_text`로 자동 폴백한다.
- **후쿠오카·도쿄를 완전판으로 전환** — 단일 일정만 있던 라이트판(330줄)을
  오사카와 같은 1인·친구·가족 3유형 구조(700줄)로 재작성.

---

## 2026-07-20

### 🚀 배포
- 프로덕션 배포 (`17954a7`, `44dfcc5`)

### ✨ 추가
- **인기 여행지 가이드 9곳 신규** — 교토·방콕·다낭·발리·싱가포르·제주·부산·강릉·파리.
  전부 오사카와 같은 구조(여행 유형 3종별 일정·예산·숙소 + 교통 + 현지팁 + 날씨).
  국내 3곳(제주·부산·강릉)은 항공권 표 대신 KTX/국내선 교통표로 대체.
- **'내 여행' 허브 `/mytrip`** — 페이지마다 흩어져 있던 저장 항목(찜한 장소·즐겨찾기 편명·
  체크리스트·검색기록)을 한 화면에 모으고, base64 URL/QR로 다른 기기에 옮길 수 있게 했다.
  로그인·DB가 없어 자동 동기화가 구조적으로 불가능한 것에 대한 대안.
- **관광지·맛집 찜** — 즐겨찾기가 항공편에만 있어 발견한 장소가 사라지던 문제.
  `tripguide_saved_places`에 저장, 홈에 찜 목록 섹션 추가.
- **목적지 날씨 위젯** — 인천공항에만 있던 날씨를 목적지 페이지 히어로에 이식(Open-Meteo).
- **홈 검색위젯·환율계산기 상태 유지** — 재방문 시 도착지·인원·금액·통화를 복원(날짜는 제외).

### 🐛 수정
- **게이트 변경 알림 문구 정직화** — "알림을 받을 수 있다"고 안내했지만 실제로는
  탭이 열려 있을 때만 동작하는 폴링이었다(백엔드·DB가 없어 Web Push 구현 불가).
  문구를 사실대로 바꾸고, 잠긴 폰에도 도달하는 유일한 경로인 ICS 캘린더 알림을 강화
  (출발편은 공항출발·게이트이동 2개 알람).
- **체크리스트 저장 키가 항목 텍스트를 포함**해 문구만 바꿔도 체크가 풀리던 문제 →
  위치 기반 안정 키(`d{그룹}_{항목}`)로 변경. 개인 항목 추가 기능도 함께 추가.
- **에러에 재시도 버튼 없음** — 환율·국내여행·마이리얼트립 검색이 실패하면
  텍스트만 뜨고 처음부터 다시 입력해야 했다. 원클릭 재시도 버튼 3곳 추가.

---

## 2026-07-16

### 🚀 배포
- 프로덕션 배포 (`7234675`, `009b587`, `c0456b4`)

### ✨ 추가
- **원클릭 앱 설치 버튼** — Chrome은 `beforeinstallprompt`, iOS Safari는 미지원이라 안내 시트로 대체.
- **법적 페이지**(개인정보처리방침·이용약관) — 제휴 마케팅 심사 대비.
- **목적지 가이드 후쿠오카·도쿄**, OG 소셜 이미지(1200×630), JSON-LD 구조화 데이터, favicon.
- **프로덕션 스모크 테스트**(`scripts/smoke_test.mjs`) — 배포 후 페이지·API 자동 점검.

### ⚡ 성능
- Chart.js 지연 로딩 + 전역 에러 핸들러 추가.

---

## 2026-07-14

### 🐛 수정
- **푸터 인기여행지 죽은 링크 제거** — 과거 revert로 페이지가 사라졌는데 링크만 남아
  21곳이 404였다. `tours.html?city=` 검색으로 교체.

---

## 2026-07-10

### ✨ 추가
- **Chrome PWA 설치 지원** — manifest·Service Worker 정비.

### 🎨 개선
- 인천공항 모바일 시인성 개선.

---

## 2026-07-07

### ✨ 추가
- **마이리얼트립 실시간 검색**(`tours.html`) — 투어·티켓·숙소·국내/국제 항공.
  인증이 필요 없는 공개 MCP 서버를 Edge Function으로 프록시.
- **메타서치 제휴 파트너 레지스트리** — 클룩·KKday·부킹닷컴·아고다.
  이들은 공개 검색 API가 없어(클룩은 서버측 403 실측) 라이브 결과 대신 검색어를 넘기는 딥링크로 확정.
- 검색 히스토리, 항공편 정렬, 날짜별 최저가 캘린더.

---

## 2026-06-30 ~ 2026-07-02

### ✨ 추가
- **이용가이드 페이지**(`guide.html`) — 8개 섹션, 사이드바 TOC, 반응형.
- 메인 페이지에 환율 계산기 복원(이전 revert 때 함께 사라졌던 기능).

---

## 2026-06-26

### 🔒 보안
- **TourAPI 키를 Edge Function 뒤로 숨김**(`api/tour.js`) — 공개 저장소라 클라이언트에
  키가 노출돼 있었다. 엔드포인트 화이트리스트 + Edge Cache(5분~1시간) 도입.

### 🎨 개선
- **인천공항 모바일 UI 대폭 개선** — 44px 터치 타깃, 가로스크롤 탭, iOS 확대 방지 16px 입력,
  safe-area, 풀스크린 모달.
- 편명 자동완성, ICS 캘린더 다운로드, URL 딥링크(`#tab=`), 한/영 토글.

---

## 참고

- 완성도 점검 기록(`COMPLETENESS.md`)은 아직 없다. 점검 자체는 이전에 수행했으나
  (`PROJECT_STATUS.md`에 결과 요약이 남아 있다) 회차 문서로 정리하지 않았다.
  다음 점검 때 `webapp-completeness-audit` 스킬로 1차 회차부터 작성할 것.
- 프로젝트 상태·구조·하지 말 것·구조적 한계는 `PROJECT_STATUS.md` 참조.
